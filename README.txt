RENDER SERVER

Runtime: Node
Build command: npm install
Start command: npm start
Health path: /health

After deployment, copy the Render URL and change https:// to wss:// for client/config.js.
The server has no external runtime dependencies.
