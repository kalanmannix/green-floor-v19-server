services:
  - type: web
    name: green-floor-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    healthCheckPath: /health
    plan: free
