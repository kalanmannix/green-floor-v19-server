# Green Floor V19 — Render/GitHub upload

Upload all files in this folder to the ROOT of the existing GitHub repository, replacing the old server files.
Render settings stay:
- Build: `pip install -r requirements.txt`
- Start: `python server.py`
- Health check: `/health`

V19 has one automatic shared world. It intentionally ignores/removes room codes and passwords.
